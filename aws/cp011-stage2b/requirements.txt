# CP-011 Stage 2B worker — validated against commit 2b9db61 / methodology_v2_freeze.json
# (python 3.13.3, pandas 2.3.1, numpy 2.3.2). Versions PINNED: the frozen
# gcp_dmv_osc functions depend on pandas 2.3.1 groupby.apply behavior (grouping
# columns preserved); newer pandas drops them -> KeyError 'slug'.
pandas==2.3.1
numpy==2.3.2
pyarrow>=15.0
asyncpg>=0.29
SQLAlchemy>=2.0
pg8000>=1.30
python-dotenv>=1.0
boto3>=1.34
